# 拾影路线图

路线图描述计划方向，不代表对应能力已在当前安装包中提供。照片安全规则适用于所有阶段：拾影不会自动删除照片；任何文件移动都先展示方案、由用户确认并记录为可撤销操作。

## v0.5 设置与工作流

- 将设置从模态弹窗改为主窗口内的独立详情页。
- 简化页面层级，减少重复导航和常驻操作。
- 保留批量选择、批量标记、批量移动及系统回收站二次确认。

## v0.6 全窗口设置

- 设置作为顶层工作区打开，不再与照片标题栏和审片侧栏同时显示。
- 使用专属分类侧栏与宽幅详情区，减少嵌套导航和面板感。
- 返回设置前的空状态或审片状态，不中断正在进行的本地分析。

## v0.7 本地智能工作流

- 读取标准 EXIF、GPS 与部分 Nikon MakerNote 拍摄参数。
- 提供分组预设、可调色彩/推荐权重、哈希实验与完全重复识别。
- 在移动前生成按日期、时间间隔和 GPS 聚类的可编辑整理预览。
- 将 RAW、同名 JPEG 和 XMP sidecar 作为关联资产安全移动并纳入撤销历史。
- 在本机自动保存审核决定，启动时可继续上一次筛选，并通过文件指纹避免恢复到已替换照片。
- 提供分析取消、冲突操作锁定、同目录移动拦截和符号链接保护。
- 统一明亮/深色主题的组件、页面层级和交互状态，并让 macOS 原生标题栏跟随应用主题切换。
- 为分析过程提供工作区进度、当前文件和取消反馈。
- 可持久化隐藏单张照片组，并让审片进度只统计需要对比的照片。
- 支持在确认后清空当前审核决定并立即开始新一轮筛选，无需重新分析照片。
- 提供小图、大图、文件列表及连续缩略图尺寸调整，并保存用户偏好。
- 大图详情支持适合窗口、100% 像素、逐级缩放、滚动查看和双击放大。
- 大尺寸照片使用受控降采样和有限并行提取特征，RAW 等高内存格式自动降低并发。
- 缓存未修改照片的分析特征，并让超大照片组渐进渲染，避免重复分析和长时间界面阻塞。
- 精确重复检测使用分层摘要预筛，并支持在分组阶段取消操作。

## 智能整理

目标是在“筛选好照片”之外，帮助用户把一次旅行或活动中的素材整理成容易理解的目录，例如：

```text
2026-05 杭州旅行/
├── 西湖/
├── 灵隐寺/
└── 城市夜景/
```

### 信息来源

- EXIF 拍摄时间与时区
- GPS 经纬度和拍摄轨迹
- 相机、镜头与焦段
- 当前文件夹结构和文件名序列
- XMP/IPTC 关键词、评分与颜色标签
- 图像相似度和客观质量特征

### 自动分组

- 根据长时间间隔识别不同旅行、活动或拍摄日。
- 对 GPS 位置做空间聚类，识别在 A 地点、B 地点拍摄的照片。
- 经用户授权后，可通过反向地理编码把坐标转换为城市和地点名称。
- 照片没有 GPS 时，结合拍摄时间、已有目录、设备信息和视觉上下文给出候选分组。
- 先生成可编辑的整理建议，用户确认后才创建目录和移动文件。

### 安全与隐私

- 永不因为分类结果自动删除照片。
- 每次整理前展示来源、目标位置、文件数量和冲突处理方式。
- 所有移动写入历史并支持撤销。
- RAW、同名 JPEG 及 XMP sidecar 作为一组关联资产共同管理，避免拆散。
- 默认在本机提取元数据和完成聚类。
- 反向地理编码需要联网时，明确说明将发送坐标，并在用户授权后才调用服务。
- 原始 EXIF/XMP 不会在未确认时被改写。

## 后续质量增强

- 并排比较、焦点区域与高光警告
- 失焦、噪点、重复截图与相似导出版本识别
- XMP/IPTC 关键词、评分和颜色标签读取
- 地图与时间线整理预览，以及经单独授权的地点名称查询
- 规则模板、筛选/整理历史与跨批次项目
- 持久化缩略图缓存和多机型千张 RAW 性能基准
- 持续跟进新相机 RAW 格式与 LibRaw 支持

# 拾影项目规划

## 1. 产品目标

帮助普通摄影用户在本地快速处理 1000 张以上的旅行照片。程序先把连拍和同机位近似照片归组，再让用户逐组审批；只有明确标记为“保留”的照片会被移动到目标文件夹。

设计原则：

- 本地优先：照片不上传，离线可用。
- 人工最终决定：算法推荐，不擅自删除或移动。
- 原件安全：不覆盖同名文件，记录移动历史，支持撤销。
- 零自动删除：排除和算法推荐不删除文件；手动删除也只进入系统回收站并强制确认。
- 跨平台：Windows 和 macOS 使用同一套业务代码，各自在目标系统打包。

## 2. 第一版 MVP

### 已实现

- 文件夹导入与拖放，递归扫描常用图片格式。
- 使用 LibRaw/rawpy 读取主流相机 RAW，优先提取内嵌预览并保留完整原文件。
- 读取图片尺寸、EXIF 拍摄时间、相机/镜头参数、GPS、清晰度和曝光指标。
- 使用“时间窗口 + 感知哈希 + 色彩直方图”聚类近似照片。
- 可调节相似度阈值与连拍时间范围。
- 分组列表、缩略图网格、大图前后翻看。
- 单张保留/排除，以及整组和推荐照片快捷操作。
- 跨照片组多选，以及批量保留、排除、移动和回收站操作。
- 自定义目标文件夹、批量移动、重名保护、最近一批移动撤销。
- 逐张手动移到系统回收站，操作前强制二次确认；不提供永久删除。
- PyInstaller 打包配置与 GitHub Actions 双平台构建流程。

### 验收标准

- 选择含 1000 张 JPG/HEIC 的目录时，界面保持可响应并显示进度。
- 高度近似的连拍照片进入同组，阈值可由用户修正。
- 未标记为“保留”的文件不会被移动。
- 两个同名文件移动到同一目标时都能保留。
- 最近一批移动可恢复到原始目录。

## 3. 算法路线

第一版采用轻量算法，安装体积和 CPU 占用可控：

1. 优先读取 EXIF `DateTimeOriginal`，缺失时使用文件修改时间。
2. 把图片缩小后生成 64 位差异哈希，识别整体构图近似程度。
3. 生成 RGB 色彩直方图，降低光线或局部细节变化造成的误差。
4. 在时间邻域内计算综合相似度，以并查集形成照片组。
5. 依据边缘清晰度和曝光偏差给组内照片排序，仅显示“推荐”。

第一版不会做人脸身份识别，也不会尝试判断照片的情绪或叙事价值。这些主观维度仍交给用户。

## 4. 后续迭代

### V0.2 审阅效率

- 会话保存与恢复，支持跨多天完成筛选。
- 星级、颜色标签、二审/终审阶段。
- 100% 放大、焦点区域对比和并排比较。
- 键盘优先的快速筛选流程。

### V0.3 智能质量检测

- 人脸与闭眼检测、多人合照表情评分。
- 明显失焦、过曝、欠曝和重复截图识别。
- 可选的本地 CLIP 模型，用于跨时间的语义聚类。
- RAW+JPEG 成对审核、XMP sidecar 联动移动与厂商新机型兼容测试。

### V1.0 发布

- Windows 安装包和签名。
- macOS `.app` / DMG、公证与签名。
- 崩溃日志脱敏、性能基准和真实照片集回归测试。

## 5. GitHub 发布计划

项目已包含双平台构建工作流。上传前需要确定 GitHub 仓库归属和可见性；上传后可推送 `v0.1.0` 标签，GitHub Actions 会分别在 Windows 与 macOS 环境构建产物。正式给他人分发前仍建议配置 Windows 代码签名和 Apple Developer 签名，否则操作系统会显示未知开发者警告。
